# Core ML/DL libraries
torch>=2.0.0
transformers>=4.35.0
peft>=0.6.0
datasets>=2.14.0
accelerate>=0.21.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# HuggingFace integration
huggingface-hub>=0.17.0
safetensors>=0.3.0

# Quantization (optional, for model optimization)
bitsandbytes>=0.41.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
packaging>=20.0

# Note: For CUDA support, install PyTorch separately with CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130

